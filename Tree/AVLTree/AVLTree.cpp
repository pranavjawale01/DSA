#include<iostreeam>

using namespce std;

class Tree {
    int data;
    Tree* left;
    Tree* right;
    left(nullptr) , right(nullptr);
};

class AVLFunc {
    Tree root;
    AVLFunc () {
        root = nullptr;
    }

    void insert(int data) {
        if (root == nullptr) {
            
        }
    }
};